# Free LLM Translator

[English](./README_en.md)

基于大语言模型的命令行翻译工具，支持[阿里云千问百炼（Qwen）](https://bailian.console.aliyun.com/)和[百度翻译API](https://fanyi-api.baidu.com/product/13)**（每月100万字符免费额度）**。

专为开发者设计，可在终端快速翻译，结果自动复制到剪贴板，极大提升工作流效率。

## ✨ 特性

- **多模型支持**：默认集成通义千问（Qwen-Plus），亦支持百度通用翻译API。
- **极速体验**：流式输出翻译结果，无需漫长等待。
- **无缝集成**：翻译结果自动复制到剪贴板，随译随用。
- **跨平台脚本**：提供 `.bat` (Windows CMD) 和 `.ps1` (PowerShell) 启动脚本。
- **彩色终端**：清晰区分思考过程与翻译结果。

![Free LLM Translator Screenshot](./ScreenShot.png)

## 📦 安装与配置

1. **克隆项目**
   ```bash
   git clone https://github.com/iChenwin/FreeLLMTranslator.git
   cd FreeLLMTranslator
   ```

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

3. **配置 API Key**
   在项目根目录创建或修改 `config.json`：
   ```json
   {
       "current_provider": "baidu",
       "providers": {
           "qwen": {
               "api_key": "sk-your-qwen-api-key",
               "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1",
               "model": "qwen-plus"
           },
           "baidu": {
               "appid": "your-baidu-appid",
               "api_key": "your-baidu-secret-key",
               "model_type": "llm"
           }
       }
   }
   ```

## 🚀 使用方法

### 基础用法
在终端直接运行脚本并跟上需翻译的文本：

**Windows (CMD/PowerShell)**
```bash
# 翻译简单短语
atrans 你好，世界！

# 翻译长句（建议使用引号包裹）
atrans "总结这篇文章，帮我列出提纲，并进行深度分析。"
```

### 快捷指令 (推荐)
将项目目录添加到系统环境变量 `PATH` 中，即可在任何位置直接使用 `atrans` 命令。

## 🛠️ 高级选项

### 切换翻译源
修改 `config.json` 中的 `current_provider` 字段即可切换服务商：
- `"qwen"`: 使用通义千问（推荐，语义理解更强）
- `"baidu"`: 使用百度翻译（免费额度大）
